{
  "$schema": "https://deno.land/x/denon/schema.json",
  "scripts": {
    "start": "deno run --allow-net --allow-read --allow-env --unstable --no-check src/server.ts"
  },
  "watcher": {
    "match": ["src/**/*.*", "public/windi.css"]
  }
}
